import { Analytics } from '@vercel/analytics/next'
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Playfair_Display } from 'next/font/google'
import './globals.css'

const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] })
const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})
const playfair = Playfair_Display({
  variable: '--font-playfair',
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
})

export const metadata: Metadata = {
  title: 'Babel — Joyería de Compromiso para Parejas Guatemaltecas',
  description: 'Descubre nuestra colección de anillos y sets de compromiso en oro y plata. Joyas que hablan por ti. Calidad, elegancia y tradición para parejas guatemaltecas.',
  keywords: ['joyería', 'anillos de compromiso', 'Guatemala', 'oro', 'plata', 'parejas', 'bodas'],
}

export const viewport = {
  themeColor: '#C9A84C',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="es" className={`${geistSans.variable} ${geistMono.variable} ${playfair.variable} bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
